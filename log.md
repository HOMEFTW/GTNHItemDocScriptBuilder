# 开发日志

## 2026-09-24：beta3 基线与 IDE 工作区
- 基线设为 GTNH `2.9.0-beta-3`，按 manifest 核验 GT Addon `2.3.4` 的 RA2 与 RecipeRemover 接口；核心生成语法不变。
- 将两排密集工具栏和固定三栏改为脚本目录/物品索引侧栏、脚本/配方设计页签、底部配方导航/草稿暂存；增加文件菜单、可调整分隔、行号、就地查找、状态栏和未保存标记。
- 新建改为未命名缓冲区；Ctrl+S 在文本编辑时也能保存，成功不再弹窗。新建、打开和退出前提供保存/放弃/取消，保存失败或取消路径不丢失当前脚本。
- 脚本页按光标识别配方类型；设计页保留按类型解析。导航刷新和原配方替换检查内容版本，避免使用过期 offset。
- 生成预览改为只读；保存保留文件尾部空白。独立编辑窗口关闭时立即同步最后输入，保存实际写文件，取消时保持窗口。
- 默认 `1440x900`、最小 `1000x700`；预览随窗口尺寸保留空间。通过实际 Windows 截图检查脚本页和配方页，修复预览父级归属和小窗口高度问题。
- 验证：原 111 项测试中有一条关于版本号断言过期，已按实际 `1.2.0` 更新；最终全量 127 项运行有 126 项通过，唯一小窗口高度回归修复后重跑全部 16 项 IDE 回归通过，复用其他 111 项已通过结果。PyInstaller 打包成功，两个仓库 `git diff --check` 通过。
- 限制：工作区当前为单文档，不是多文件编辑页签；草稿暂存仍仅本会话有效。未在 GTNH beta3 游戏中执行生成脚本。
- 保留原有修改与本地索引，未提交、推送或发布。

## 2026-05-03: 关于对话框和应用信息

### 已完成
- 参考 `gtnh-mod-installer` 的关于对话框样式，新增 `gui.dialogs.AboutDialog`。
- 关于对话框显示应用名 `GTNHItemDocScriptBuilder`、版本 `1.0.0`、用途说明、`工作室 Andgatech` 和 `© 2026`。
- 在窗口右上角新增“关于”按钮，点击后打开应用信息对话框。
- 补充右上角按钮位置和关于对话框内容测试。

### 决策
- 版本号固定为用户指定的 `1.0.0`；对话框使用独立 `Toplevel`，不复用普通消息框，便于后续扩展图标或更多应用信息。

---

## 2026-05-03: 手写切换清空和应用图标

### 已完成
- 手写模式下切换“脚本类型”会清空所有配方格和流体行，避免旧类型内容误带入新类型。
- 解析模式保持原逻辑：切换脚本类型时按当前类型自动重新解析完整 `.zs`。
- 窗口启动时使用项目根目录的 `icon.ico` 设置 Tk 标题栏图标。
- `build.spec` 增加 `icon="icon.ico"`，打包后的 exe 使用同一个图标。
- `build.spec` 的 datas 增加 `("icon.ico", ".")`，保证 onefile 运行时也能找到图标资源。
- 补充手写切换清空、窗口图标、PyInstaller 图标配置测试。

### 遇到的问题
- **只给 PyInstaller 设置 `icon` 不保证 Tk 运行时能找到图标**：onefile 运行时需要把 `icon.ico` 作为 data 一起打包 → 同时添加到 `datas`。

### 决策
- 手写模式切换类型不再迁移旧格子；需要保留多条临时内容时使用“草稿列表”。

---

## 2026-05-03: 加高下方三栏工作区

### 已完成
- 将默认窗口高度从 `1500 x 980` 调整为 `1500 x 1080`。
- 将最小窗口高度从 `1400 x 960` 调整为 `1400 x 1040`。
- 旧保存配置的高度低于 `1040` 时会自动提升，避免下方三栏太短。
- 保持上方“配方列表”4 行和“草稿列表”3 行不变，新增高度留给下面左侧搜索、中间编辑和右侧预览三栏。
- 补充默认高度、最小高度、上方列表固定高度的布局测试。

### 决策
- 只增加高度，不调整宽度和三栏比例，继续遵守左侧 `580px`、右侧 `400px`、中间吃剩余宽度的 UI 约束。

---

## 2026-05-03: 脚本草稿列表

### 已完成
- 新增“草稿列表”区域，显示草稿序号、脚本类型和智能摘要。
- 新增“保存草稿”按钮，可把当前可生成草稿保存到列表。
- 新增“载入草稿”按钮和双击/回车载入能力，可把选中草稿回填到 GUI。
- 新增“删除草稿”按钮，可移除选中草稿并重新编号。
- 新增“全部添加”按钮，可按列表顺序把所有草稿追加到完整 `.zs` 脚本。
- 草稿保存时使用 `deepcopy(RecipeDraft)`，避免后续编辑当前 GUI 时改动已保存草稿。
- 补充保存/载入、删除、全部追加的 GUI 测试。

### 遇到的问题
- **草稿必须保存当时状态**：直接引用当前 `RecipeDraft` 会被后续 UI 操作污染 → 使用 `deepcopy` 保存独立副本。

### 决策
- 草稿列表先作为当前会话内的临时工作区，不写入磁盘；真正持久化仍通过右侧完整 `.zs` 文件保存。

---

## 2026-05-03: 生成前草稿校验

### 已完成
- `添加到脚本` 和 `替换原配方` 改为使用同一个草稿生成/校验路径。
- 当前草稿无效时只更新下半区错误预览和状态栏，不再把 `// 无法生成脚本...` 插入完整脚本。
- 数字字段增加中文错误提示：`Duration`、`EU/t`、`燃烧时间`、`Special Value` 和熔炉 XP 会区分空值、整数或数字格式错误。
- 保存完整 `.zs` 文件不被当前草稿校验拦截，保留手写完整脚本的自由度。
- 补充无效草稿添加、无效草稿替换、非法 `Duration` 的回归测试。

### 遇到的问题
- **旧逻辑会把错误预览当作脚本插入**：`_refresh_preview()` 捕获异常后仍会让 `添加到脚本` 读取错误文本 → 新增 `_generate_current_draft_script(blocking=True)`，失败时返回 `None`。

### 决策
- 生成前校验只作用于“把当前草稿写入完整脚本”的动作；“保存”继续保存完整脚本原文，不校验当前草稿。

---

## 2026-05-03: 新建脚本和当前文件保存

### 已完成
- 新增“新建 .zs”按钮，可在未导入脚本时选择路径并创建空 `.zs` 文件。
- 新增当前脚本路径状态，导入或新建后工具栏会显示当前文件路径。
- 将“保存 .zs”调整为“保存”：已有当前文件时直接覆盖，不再每次弹出另存为窗口。
- 新增“另存为”按钮，可把完整脚本保存到新路径，并把新路径设为当前文件。
- 未绑定当前文件时点击“保存”会自动走“另存为”，保持空白编写模式可创建脚本文件。
- 补充新建、导入后直接保存、未绑定路径保存回退到另存为的 GUI 测试。

### 遇到的问题
- **旧保存逻辑每次都弹保存窗口**：导入脚本后无法像 IDE 一样直接覆盖原文件 → 增加 `current_script_path` 并区分 `保存` 与 `另存为`。

### 决策
- “保存”始终保存右侧上半区完整 `.zs` 文件；当前草稿仍需先通过“添加到脚本”进入完整脚本，避免误把临时草稿自动写入文件。

---

## 2026-05-03: 简化机器生成方式

### 已完成
- 将机器参数区“模板”改名为“生成方式”，降低它和 `Recipe Map` 的概念冲突。
- 简化生成方式下拉框：只保留 `GT RA2`、`Thermal Expansion Furnace`、`Thermal Expansion Pulverizer`、`AE Grinder`、`AE Inscriber`。
- 移除界面上的 `Assembler-like`、`Cutter-like`、`Macerator-like`、`Mixer-like`、`Chemical Reactor-like`、`Blast Furnace-like` 重复选项。
- 增加旧模板 ID 兼容层：旧配置或旧草稿仍可生成脚本，并保留旧模板对应的默认 `Recipe Map`。
- 补充生成方式列表、旧模板迁移和 GUI 标签测试。

### 遇到的问题
- **旧模板曾经同时承担“生成语法”和“默认 recipe map”两种职责**：界面简化后需要避免旧配置丢失 `cuttingsaw` 等默认 map → 增加 `LEGACY_GT_TEMPLATE_RECIPE_MAPS` 迁移表。

### 决策
- 后续 GT 机器只通过 `Recipe Map` 选择具体机器；“生成方式”只负责决定生成 RA2、Thermal Expansion 或 AE 语法。

---

## 2026-05-03: 智能摘要和自动中文注释

### 已完成
- 调整自动注释格式：有中文名的物品/流体会显示为 `中文名 <ct表达式> * 数量`。
- 配方列表摘要增强：合成显示 `输出 <- 输入`，GT 显示 `recipe map: 输入 -> 输出`，删除显示删除目标。
- 当前草稿预览自动在脚本前生成中文注释。
- 有序/无序/熔炉/燃料/删除/GT 机器都使用统一的草稿描述逻辑。
- `ScriptFluid` 增加 `comment_name`，流体搜索填入时可保留中文名用于注释。
- 补充配方摘要和自动注释测试。

### 遇到的问题
- **手写流体没有中文名**：手动输入只知道流体 ID → 注释会回退到流体名；通过搜索填入的流体会使用导出数据里的中文名。

### 决策
- 自动注释放在 GUI 草稿预览层，不改变底层 `ZsGenerator.generate()` 的纯脚本生成接口。

---

## 2026-05-03: 脚本插入位置和原配方替换

### 已完成
- “添加到脚本”新增“添加位置”选择：文件末尾、当前光标、当前配方后。
- 新增“替换原配方”按钮，可把当前解析配方的原始 `.zs` 片段替换成 GUI 当前草稿生成内容。
- `ParsedRecipe` 增加 `start_offset` 和 `end_offset`，用于安全定位完整脚本中的原配方范围。
- parser 可识别普通函数调用和 RA2 builder 到 `.addTo(...)` 的替换范围。
- 补充 source offset、当前光标插入、当前配方后插入和替换原配方测试。

### 遇到的问题
- **替换不能靠文本搜索**：同一个输出或相似配方可能重复出现 → parser 返回起止字符 offset，GUI 按 offset 替换。

### 决策
- “当前配方后”在原配方片段结束后插入新草稿；“替换原配方”只替换 parser 定位到的原始片段，保留前后注释和其它配方。

---

## 2026-05-03: 当前类型配方列表

### 已完成
- 新增上方“配方列表”，按当前脚本类型列出完整 `.zs` 中所有可解析配方。
- 列表显示当前类型序号、行号、脚本类型和摘要。
- 点击列表行会直接解析对应配方并填回 GUI。
- `core.zs_parser` 新增 `parse_zs_script_matches`，复用现有 parser 生成可点击列表数据。
- 补充 parser 列表测试和 GUI 行点击解析测试。

### 遇到的问题
- **只有上一条/下一条仍不方便定位**：大脚本需要看见所有当前类型匹配项 → 增加 Treeview 列表显示摘要。

### 决策
- 配方列表只显示当前脚本类型的匹配结果，避免有序、无序、GT 混在一起造成误选。

---

## 2026-05-03: GT 机器流体栏动态条数

### 已完成
- 将 GT 机器普通“流体输入”和“流体输出”从单行控件改成动态多行控件。
- 输入和输出各自拥有独立“条数”输入框，默认 1 条，互不影响。
- 填写条数后会动态生成对应数量的 UI 行，每行保留搜索、数量和清空能力。
- 导入已有脚本时，如果流体数量超过默认 1 条，会自动扩展到脚本里的数量。
- 补充动态流体条数、独立输入/输出条数和导入扩展测试。

### 遇到的问题
- **固定 4 条不符合新需求**：用户改为希望通过输入框自定义条数 → 保留 `FluidListRowsFrame` 的固定模式给 GT 删除使用，新增 `adjustable=True` 模式给普通 GT 机器输入/输出使用。

### 决策
- GT 删除流体输入继续固定 4 条；普通 GT 机器流体输入/输出改为默认 1 条并按用户填写动态扩展。

---

## 2026-05-03: 修复解析模式切换和紧凑工具栏

### 已完成
- 新增 `.zs` 解析导航按钮：“第一条”“上一条”“下一条”“最后一条”。
- `parse_zs_script_with_source` 增加 `occurrence` 参数，可按当前脚本类型解析第 N 条或最后一条匹配配方。
- 右侧当前草稿标题会显示当前类型序号、当前类型总数、全脚本受支持配方序号和行号。
- 将顶部工具栏改为两排按钮：第一排放索引、导入、解析、添加和保存，第二排放撤销、重做、刷新、复制、清空和 OreDict。
- 将索引按钮文字从“选索引”恢复为“选择 item_index.json”。
- 将默认窗口高度从 `920` 提高到 `980`，最小高度和旧配置归一化高度从 `900` 提高到 `960`。
- 调整顶部工具栏按钮宽度算法，中文按显示宽度计算并留出余量，避免按钮文字被压缩到难以辨认。
- 为工具栏按钮增加轻量 padding，在保持小间距的同时提升可读性。
- 新增解析模式状态：点击“解析到GUI”成功后开启，切换脚本类型或删除子类型会按当前类型重新解析完整脚本。
- 新增“关闭解析”按钮，用于回到手动草稿模式。
- 修复解析过有序/无序合成后切换到 GT 时旧格子被迁移到 GT 布局的问题。
- 顶部工具栏按钮改为紧凑宽度和小间距。
- 补充解析模式切换、关闭解析、工具栏紧凑度测试。

### 遇到的问题
- **旧草稿被误迁移到新脚本类型**：`_on_recipe_kind_selected()` 总是把当前格子复制到新布局 → 解析模式开启时改为直接按当前脚本类型重新解析完整脚本。
- **只能解析第一条同类型配方**：parser 的具体解析函数都从文本开头找第一个调用 → 选择目标位置后把该位置之后的脚本文本传给具体解析函数。

### 决策
- “导入 .zs”仍然不自动解析；只有点击“解析到GUI”后才开启解析模式，用户可随时用“关闭解析”回到手动编写。
- 工具栏优先保证按钮文字可读；通过两排布局消化按钮数量，而不是继续压缩按钮文字。

---

## 2026-05-03: 重做 ZS 编辑工作流

### Completed
- “导入 .zs”改为只打开完整脚本，不再自动解析到 GUI。
- 新增“解析到GUI”按钮，按当前脚本类型过滤解析目标。
- 修复有序/无序脚本可能被解析成 GT 配方的问题。
- 新增“添加到脚本”按钮，将当前草稿生成内容追加到完整 `.zs` 文件编辑区。
- “保存 .zs”改为保存完整文件编辑区，而不是只保存当前草稿。
- 新增“撤销”和“重做”按钮，作用于完整脚本编辑区。
- 补充按脚本类型解析、手动解析、添加到脚本、保存完整文件、撤销/重做测试。

### Issues Encountered
- **导入自动解析不符合写代码习惯**：打开脚本时会立即改 GUI 草稿 → 改为只加载完整文件，解析由按钮触发。
- **解析缺少脚本类型约束**：parser 只按文本顺序找第一条支持调用 → 增加 `allowed_kinds`，由当前 GUI 脚本类型决定可解析目标。
- **保存语义错误**：保存按钮原先保存当前草稿生成内容 → 改为保存完整文件编辑区。

### Decisions Made
- 完整脚本编辑区是主文件内容；当前草稿只是待追加片段，必须点击“添加到脚本”才进入完整文件。

---

## 2026-05-03: 右侧 ZS 预览上下分栏

### Completed
- 将右侧 `PreviewFrame` 分成上下两半。
- 上半区显示导入的完整 `.zs` 文件原文。
- 下半区显示当前草稿会保存出来的内容。
- 两个预览区都支持竖向和横向滚动条。
- 导入脚本后，下半区标题显示文件名、第几条受支持配方、起始行号和调用类型。
- `parse_zs_script_with_source` 增加来源信息，方便说明当前草稿对应完整脚本中的哪一条配方。

### Issues Encountered
- **完整脚本里有很多配方时当前草稿不清楚**：原右侧只显示当前草稿生成结果，无法知道它来自导入脚本的哪一段 → 上半区保留完整脚本，下半区标题标注来源。

### Decisions Made
- 保存和复制仍使用下半区“当前草稿保存内容”；上半区只是完整导入文件参考，不会被保存按钮直接写出。

---

## 2026-05-03: 修复导入后的 ZS 预览语义

### Completed
- `parse_zs_script` 改为按脚本出现顺序选择第一条受支持配方。
- 修复完整脚本里后面的 `RecipeRemover.remove(...)` 抢先于前面配方被导入的问题。
- 脚本类型切换时迁移当前格子到新布局，不再恢复隐藏布局里的旧草稿。
- 补充 parser 顺序解析测试和 GUI 单草稿预览测试。

### Issues Encountered
- **不同脚本类型像不同预览**：每个布局保留隐藏格子，切回旧类型会恢复旧内容 → 切换类型时把当前草稿迁移到目标布局。
- **导入完整脚本时选择错误配方**：parser 用固定类型优先级检查调用 → 改为按文本位置选择第一条受支持调用。

### Decisions Made
- ZS 预览只代表当前这一份草稿，不再让隐藏布局表现为多份并行草稿。

---

## 2026-05-03: ZS 导入继续编辑 MVP

### Completed
- 新增 `core.zs_parser.parse_zs_script`，可解析第一条受支持的 `.zs` 配方。
- 支持导入本工具生成的 GTNH RA2 builder、GT `RecipeRemover.remove(...)`、有序/镜像有序/无序合成、熔炉、燃料和删除脚本。
- 工具栏新增“导入 .zs”，读取脚本后将 `RecipeDraft` 填回当前 GUI 草稿。
- 导入后可继续编辑物品格、流体、`Recipe Map`、`Duration`、`EU/t`、输出概率等参数。
- 补充 parser 和 GUI 回填测试，覆盖关闭后重新导入继续编辑的场景。

### Issues Encountered
- **已有脚本不是结构化存档**：`.zs` 可能包含任意 ZenScript → MVP 只做静态解析，不执行脚本，只恢复第一条匹配的受支持配方。
- **真实脚本存在 `<ore:...>*0` 无空格写法**：数量解析不能依赖 ` * ` 固定空格 → parser 支持紧贴的 `*0`。

### Decisions Made
- 先不做多配方列表；导入完整脚本时只取第一条受支持配方，后续再做多草稿选择。

---

## 2026-05-03: GT 删除配方多流体输入

### Completed
- 新增 `FluidListRowsFrame`，用于多行流体输入，每行支持搜索、数量和清空。
- GT 删除模式改用独立“GT 删除流体输入”列表，普通机器流体输入不再在删除模式下显示。
- `RecipeRemover.remove(...)` 可从 16 个物品输入格和 4 行流体输入生成物品数组、流体数组。
- 补充 GUI、组件和生成器测试。

### Issues Encountered
- **GT 删除配方可能有多条流体输入**：原先复用单行 `FluidListFrame`，只能生成一条流体 → 为删除模式增加专用多行列表。

### Decisions Made
- GT 删除的物品输入继续使用现有 16 格输入区，避免重复造一套物品选择逻辑；只单独拆出流体输入数组编辑。

---

## 2026-05-03: 中间编辑栏竖向滚动条

### Completed
- 将中间编辑区内容包入 `Canvas` 滚动容器。
- 增加中间编辑栏竖向滚动条，参数和格子过多时可以向下查看。
- 补充 GUI 布局测试，确认滚动条存在且不改变左侧 `580px`、右侧 `400px` 固定宽度。

### Issues Encountered
- **中间栏内容逐渐变长**：RA2 参数和 9 输出格增加后，固定窗口高度下部分控件容易看不到 → 给中间栏增加独立竖向滚动。

### Decisions Made
- 只添加中间栏竖向滚动条，不增加横向滚动条；宽度仍由既定三栏比例控制。

---

## 2026-05-03: GT 机器输出格调整为 9 格

### Completed
- 将 `machine` 布局的物品输出格从 4 格改为 9 格。
- 输出格排列改为 `3 x 3`，标题改为“机器输出格 9 格”。
- 更新布局测试，继续保留 GT 删除布局的 4 个目标输出格。

### Issues Encountered
- **GT 实际输出 UI 是 9 格**：原 GUI 只给 4 个输出格，无法覆盖更多输出配方 → 扩展机器配方输出布局。

### Decisions Made
- 只调整 `machine` 配方布局；`remove_machine` 仍保持 4 个目标输出格，避免改变现有删除编辑语义。

---

## 2026-05-03: RA2 Special Item 支持

### Completed
- 为 `RecipeDraft` 增加可选 `special_item`。
- GTNH/模组机器生成器支持 `.specialItem(item)`，空值时不生成该调用。
- GUI 在 GTNH/模组机器参数区增加 `Special Item` 行，可从当前选中物品格复制物品、数量和后缀。
- 增加清空 `Special Item` 按钮。
- 补充生成器和 GUI 测试。

### Issues Encountered
- **`specialItem` 需要复用物品表达式能力**：手写容易丢失 `*0` 和 `.withTag(...)` → 直接复制现有 `ScriptItem`。

### Decisions Made
- `Special Item` 只在 GTNH/模组机器显示；切换其他脚本类型时不参与生成。

---

## 2026-05-03: RA2 Special Value 支持

### Completed
- 为 `RecipeDraft` 增加可选 `special_value`。
- GTNH/模组机器生成器支持 `.specialValue(value)`，空值时不生成该调用。
- GUI 在 GTNH/模组机器参数区增加 `Special Value` 输入框，只在机器配方显示。
- 补充生成器和 GUI 测试。

### Issues Encountered
- **`specialValue` 只适用于 RA2 机器配方**：删除、合成、熔炉、燃料都不应显示该参数 → 复用机器参数显隐规则。

### Decisions Made
- `Special Value` 使用整数原样生成，不做业务含义解释；不同 recipe map 的特殊值语义留给用户按 GTNH 脚本规则填写。

---

## 2026-05-03: RA2 输出概率支持

### Completed
- 为 `RecipeDraft` 增加 `output_chances`。
- GTNH/模组机器生成器支持 `.outputChances([...])`，未填写概率时保持旧脚本格式不变。
- 输出格保存独立 `output_chance`，默认 `10000` 表示 `100%`。
- GUI 在 GTNH/模组机器模式下选中输出格时显示“输出概率”，选中输入格或其他脚本类型时隐藏。
- 补充生成器和 GUI 测试。

### Issues Encountered
- **输出概率只对 GT RA2 输出有效**：普通合成、熔炉和输入格不应显示该控件 → 显隐绑定到“机器模式 + 当前选中输出格”。

### Decisions Made
- 使用 GregTech 常用概率单位 `10000 = 100%`，不做百分号换算，避免和真实 `.zs` 脚本数值不一致。

---

## 2026-05-03: 参数和流体区按能力显示

### Completed
- 有序/无序合成不再显示流体输入/输出、模板、`Recipe Map`、`Duration`、`EU/t` 等无效参数。
- 熔炉只显示 XP 相关参数，燃料只显示燃烧时间。
- GTNH/模组机器显示模板、`Recipe Map`、`Duration`、`EU/t`、无流体开关和流体输入/输出。
- 删除模式仅在子类型为 `GT` 时显示模板、`Recipe Map` 和流体输入；普通有序/无序/熔炉删除不显示流体。
- 补充 GUI 测试，锁定各脚本类型的参数与流体显隐规则。

### Issues Encountered
- **有序/无序显示流体输入输出**：这些脚本语法根本不接收流体 → 将流体区限制到机器配方和 GT 删除。

### Decisions Made
- 隐藏控件时保留已填值，切换回对应类型后仍可继续编辑。

---

## 2026-05-03: MineTweaker 参数按类型显示

### Completed
- 将 MineTweaker 专项参数改为按脚本类型显示。
- 有序合成只显示“镜像有序合成”，无序合成不显示这些专项参数。
- 熔炉只显示“写入熔炉 XP”，燃料只显示“燃烧时间”。
- 补充 GUI 测试，锁定参数显隐行为。

### Issues Encountered
- **公共参数区误导性强**：`recipes.addShapedMirrored`、`furnace.addRecipe` XP 开关和 `furnace.setFuel` ticks 并不适用于所有类别 → 根据 `recipe_kind` 控制显隐。

### Decisions Made
- 保留参数值本身，不因切换类别清空；只控制显示，避免用户临时切换类型时丢配置。

---

## 2026-05-03: MineTweaker 参数左对齐

### Completed
- 将“写入熔炉 XP”、“镜像有序合成”和“燃烧时间”从参数区右侧列移动到左侧连续行。
- 增加 GUI 布局测试，约束 MineTweaker 参数控件从第 0 列开始，避免被 `Recipe Map` 宽列推远。

### Issues Encountered
- **新控件离左侧太远**：控件放在第 2/3 列，受 `Recipe Map` 下拉框宽度影响 → 改为左侧纵向排列。

### Decisions Made
- 不改变三栏比例常量，只调整参数框内部 grid 排列。

---

## 2026-05-03: MineTweaker 合成与熔炉补充

### Completed
- 为 `RecipeDraft` 增加 `shaped_mirrored`、`include_furnace_xp` 和 `fuel_ticks`。
- 生成器支持 `recipes.addShapedMirrored(...)`、两参数 `furnace.addRecipe(output, input)` 和 `furnace.setFuel(item, ticks)`。
- GUI 脚本类型增加“燃料”，布局为 1 个输入格、无输出格。
- 参数区增加“镜像有序合成”、“写入熔炉 XP”和“燃烧时间”控件。
- 补充生成器、布局和 GUI 测试，保持左侧 `580px`、右侧 `400px` 的三栏比例常量不变。

### Issues Encountered
- **真实脚本存在两参数熔炉写法**：原生成器固定输出 XP 参数 → 增加 `include_furnace_xp` 开关。

### Decisions Made
- “燃料”作为独立脚本类型处理，避免和熔炉配方共用输出格造成误填。

---

## 2026-05-03: RA2 无流体输入输出开关

### Completed
- 参考 `Minetweaker-Gregtech-5-Addon` Wiki 和源码 `RA2Builder.java`，核对 RA2 builder 可用方法。
- 为 `RecipeDraft` 增加 `no_fluid_inputs` 和 `no_fluid_outputs`。
- 在 GTNH/模组机器参数区增加“无流体输入”和“无流体输出”复选框。
- 勾选后生成 `.noFluidInputs()` / `.noFluidOutputs()`，并跳过对应的 `.fluidInputs(...)` / `.fluidOutputs(...)`。
- 补充生成器和 GUI 测试。

### Issues Encountered
- **Wiki 与当前源码不完全一致**：Wiki 文档列出 `.noOptimize()`，但当前 `RA2Builder.java` 未暴露该 `@ZenMethod` → 暂不让 GUI 生成 `.noOptimize()`。

### Decisions Made
- 先支持源码确认存在的 `.noFluidInputs()` 和 `.noFluidOutputs()`，保证生成脚本可用。
- 保持三栏比例不变；新增复选框放入中间参数区。

---

## 2026-05-03: 增加 GUI 窗口高度

### Completed
- 将默认窗口从 `1500x860` 调整为 `1500x920`。
- 将主窗口最小高度从 `760` 调整为 `900`。
- 将旧配置归一化的默认最小高度调整为 `900`，避免 `config.json` 中保存的 `1200x820` 继续让窗口偏矮。
- 更新窗口高度相关测试和文档。

### Issues Encountered
- **旧配置会覆盖默认窗口高度**：`config.json` 里保存了较矮窗口尺寸 → 通过提高 `normalize_window_geometry` 的 `min_height` 解决。

### Decisions Made
- 只增加高度，不调整左侧 `580px`、右侧 `400px` 和中间栏权重，继续保护既定三栏比例。

---

## 2026-05-03: 物品数量和 NBT 后缀支持

### Completed
- 为 `ScriptItem` 增加 `suffix` 字段，生成脚本时会拼接到物品表达式后，再追加数量。
- 调整物品数量生成逻辑，允许 `amount = 0` 输出 `<...> * 0`，用于 GT 编程电路和模具。
- 在中间编辑区增加“选中物品格”控件，可编辑当前格子的数量和 `.withTag(...)` 等后缀。
- 补充 `*0`、NBT 后缀和 GUI 选中格编辑行为测试。

### Issues Encountered
- **旧逻辑把 `0` 当作未设置数量**：`if self.amount and self.amount > 1` 会吞掉 `0` → 改为 `amount != 1` 时输出数量。

### Decisions Made
- 后缀只做原样拼接：GUI 不解析 NBT 内容，避免破坏 CraftTweaker 的原始表达式。
- 数量和后缀编辑放在中间栏内部：不改变左侧 `580px` 和右侧 `400px` 的固定比例。

---

## 2026-05-03: OreDict GUI 适配

### Completed
- 为 GUI 增加 `ore_dictionary_index.json` 自动加载流程，和 `item_index.json`、`fluid_index.json` 放在同一导出目录时会自动启用。
- 新增 OreDict 搜索弹窗，可按 `oreName`、`<ore:...>` 表达式和包含物品表达式搜索，并填入当前配方输入格。
- 补充 OreDict 索引、弹窗和主窗口填入行为测试。
- 保持三栏 UI 比例不变：左侧搜索 `580px`，右侧预览 `400px`，中间编辑区使用剩余空间。

### Issues Encountered
- **项目缺少 `log.md` 和 `context.md`**：本次按项目记录规则补齐，便于后续继续接力。

### Decisions Made
- OreDict 仅允许填入输入格：CraftTweaker 和 GregTech 配方输出通常需要具体物品，矿物字典更适合作为输入匹配。
- OreDict 搜索结果使用独立弹窗和横向滚动条：避免长物品列表撑宽主界面，保护既定三栏比例。
